---
title: "Deployment Environments"
description: "Understanding Webiny environments and deployments."
order: 3
---


# Deployment environments

In this lesson, we will explore the concept of deployment environments in Webiny. By the end of this lesson, you will understand how to manage different environments for your Webiny projects.

<ChapterOverview
    items={[
        { icon: "desktop_cloud_stack", text: "What are environments." },
        { icon: "deployed_code", text: "What are state files." },
        { icon: "deployed_code", text: "How to manage different environments" },
    ]}
/>

## What are Environments

In Webiny, an environment represents a specific deployment of your project. Each environment has its own set of resources, configurations, and state files. This allows you to have multiple instances of your Webiny project, such as development, staging, and production environments.

### Environment types

There are two main types of environments in Webiny:
- **Long lived environments**: These are persistent environments that you maintain over time, such as development, staging, and production. They are typically used for ongoing development and deployment of your Webiny project and are managed through CI/CD.
- **Ephemeral environments**: These are temporary environments that are created for specific purposes, such as testing or experimentation. They can be easily created and destroyed as needed. They are usually created on top of a code PR via the CI/CD system, or manually via the CLI by a developer working on a specific feature.

For the purposes of this course we'll mainly focus on ephemeral environments, as they are easier to create and manage for learning and experimentation.

<Quiz
    question="If you are working on a new feature and want to create a temporary environment to test it, which type of environment would you use?"
    options={[
        "Long lived environment",
        "Development environment",
        "Ephemeral environment",
        "Local environment",
    ]}
    correctAnswer={3}
    explanation="Correct! Ephemeral environments are perfect for testing and developing new features as they can be easily created and destroyed without affecting your main environments."
    hint="There are only two types of environments: Long lived and Ephemeral, can you guess which one is temporary and used for testing new features."
/>

## What are State Files

State files are JSON files that store the current state of your Webiny deployment. They contain information about the resources that have been provisioned, their configurations, and other metadata related to your environment. State files are essential for managing your Webiny environments, as they allow you to track changes and ensure consistency across deployments.

<Callout type="warning" title="Don't lose your state files!">
    State files are crucial for managing your Webiny environments. Losing them can lead to inconsistencies and potential issues during deployments or completely losing access to a deployment. Always ensure that you back up your state files and keep them in a safe location.
</Callout>

<Quiz
    question="What's the best way to share state files among team members working on the same Webiny project?"
    options={[
        "Copy them via email",
        "Store them in a shared remote storage solution like an S3 bucket",
        "Keep them on local machines",
        "Use a USB drive to share them",
    ]}
    correctAnswer={2}
    explanation="Exactly! Storing state files in a shared remote storage solution like an S3 bucket allows all team members to access and manage the same environment without conflicts."
    hint="State files should be stored in a location that is accessible to all team members, a shared remote storage solution is the best option."
/>

### Storing State Files

By default, Webiny stores state files locally within your project directory. However, for better management and collaboration, especially in team environments, it's recommended to store state files in a remote storage solution, such as an S3 bucket for which Webiny provides an out-of-the-box adapter. This allows multiple team members to access and manage the same environment without conflicts.

For the purposes of this course, we will continue to use local state files to keep things simple.

## How to Manage Different Environments

Managing different environments in Webiny is straightforward. You can create, deploy, and switch between environments using the Webiny CLI. Here are some common commands for managing environments:

- `yarn webiny deploy --env <environment-name>`: Deploys your Webiny project to the specified environment. If the environment does not exist, it will be created.
- `yarn webiny info --env <environment-name>`: Displays information about the specified environment, including endpoints and configurations.
- `yarn webiny destroy --env <environment-name>`: Destroys the specified environment and all associated resources. Use this command with caution, as it will permanently delete your deployment.

<Quiz
    question="What command would you execute to get the admin url of the `production` environment?"
    options={[
        "webiny info --env production",
        "yarn webiny deploy --env production",
        "yarn webiny info",
        "yarn webiny info --env production",
    ]}
    correctAnswer={4}
    explanation="That's right, this command will provide you with all the necessary information about the `production` environment, including the admin url and it follows the yarn webiny [command] --env [environment-name] pattern."
    hint="All webiny CLI commands follow the pattern: yarn webiny [command] --env [environment-name] and by providing the env name, that command takes that environment into account when providing the info."
/>