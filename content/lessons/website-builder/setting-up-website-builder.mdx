---
title: "Setting up Website Builder"
description: "Understand how the Website Builder connects to your frontend app, then get the starter kit running and create your first page."
order: 1
---

# Setting up Website Builder

In this lesson, we'll explore how the Website Builder works from a developer's perspective, then get the Next.js starter kit running locally and create a first page — confirming the full stack is wired up correctly.

<ChapterOverview
  items={[
    { icon: "architecture", text: "Understand how the editor and your app connect." },
    { icon: "download", text: "Clone and install the Website Builder Next.js starter kit." },
    { icon: "key", text: "Configure API keys and environment variables." },
    { icon: "web", text: "Create a page in the Admin editor and see it rendered." }
  ]}
/>

**Prerequisites:**

- A running Webiny project (completed via the Getting Started chapter, or already deployed)
- Node.js 18+ and a package manager (npm, yarn, or pnpm)
- Familiarity with Next.js App Router — the starter kit uses App Router, not Pages Router

## How It All Fits Together

Before we start writing any code, it's worth taking a moment to understand how the Website Builder actually works — because it's a bit different from what you might expect.

When a content editor opens the Website Builder in Webiny Admin, they see a canvas where they can drag components onto a page, configure their inputs in a sidebar, and hit Publish. That's the editorial experience.

<Image
  src="/images/lessons/foundation/webiny-website-builder.png"
  title="Website Builder - Page editor"
/>

On the developer side, the picture is a bit different — and it's what makes the Website Builder stand out from most page builders.

**The editor doesn't have its own components or styles.** Instead, it's always connected to a separate frontend app that you own — in our case a Next.js project. When the editor opens, it loads your Next.js app inside an iframe. The component palette, the live preview, all of it is your real app running right there.

This means all your component code and all your styles live in your Next.js project. Webiny only stores the page structure — which components are on the page and what input values the editor set for each one. It has no idea what a "Hero" or "Banner" looks like, and it never needs to.

The practical upside: no style clashes, no fighting the platform, full ownership of your code. And because editors are looking at your actual app, the WYSIWYG is genuine — not a simulation.

<Image
  src="/images/website-builder/architecture-diagram.png"
  alt="Diagram showing Webiny Admin embedding your Next.js app in an iframe, communicating via postMessage through the SDK"
  caption="The Website Builder editor loads your Next.js app in an iframe. The SDK bridges the two sides via postMessage."
/>

{/* TODO: replace architecture-diagram.png with final version */}

The `@webiny/website-builder-nextjs` SDK is what connects the two sides — it handles the communication between the editor and your app when editing, and fetches published page documents from Webiny when rendering pages for visitors.

With that in mind, let's get everything set up.

## Step 1: Clone the Starter Kit

The official starter kit lives at [github.com/webiny/website-builder-nextjs](https://github.com/webiny/website-builder-nextjs).

```bash title="Terminal"
git clone https://github.com/webiny/website-builder-nextjs.git my-website
cd my-website
npm install
```

Before running `npm install`, make sure the `@webiny/website-builder-nextjs` version in `package.json` matches your Webiny version. You can find it at the bottom of the Admin sidebar, or by running `webiny --version` in your Webiny project.

<Image
  style={{ width: 400 }}
  src="/images/website-builder/webiny-version-sidebar.png"
  alt="Webiny Admin sidebar showing the Webiny version number at the bottom"
  caption="The Webiny version is shown at the bottom of the Admin sidebar"
/>

For example, if your Webiny version is `6.0.0`, your `package.json` should have:

```json title="package.json"
{
  "dependencies": {
    "@webiny/website-builder-nextjs": "6.0.0"
  }
}
```

## Step 2: Gather Your Credentials

To connect the starter kit to your Webiny project, you'll need an API key, API host URL, and tenant ID. The easiest way to get these is through the **Configure Next.js** shortcut in Webiny Admin — click **Support** in the bottom-left corner and select **Configure Next.js**.

<Image
  src="/images/website-builder/configure-nextjs-menu.png"
  alt="Webiny Admin sidebar with the Support menu open, showing the Configure Next.js option"
  caption="Open the Support menu and select Configure Next.js"
/>

A dialog appears with the three environment variables already filled in and ready to copy:

<Image
  src="/images/website-builder/configure-nextjs-dialog.png"
  alt="Configure Next.js dialog showing NEXT_PUBLIC_WEBSITE_BUILDER_API_KEY, API_HOST, and API_TENANT pre-filled"
  caption="The Configure Next.js dialog gives you all three values in one click"
/>

Click the copy icon and paste the block directly into your `.env` file in the next step.

## Step 3: Configure Environment Variables

Create a `.env` file in the root of your Next.js project:

```dotenv title=".env"
NEXT_PUBLIC_WEBSITE_BUILDER_API_KEY=your_api_key_here
NEXT_PUBLIC_WEBSITE_BUILDER_API_HOST=https://your-cloudfront-url.cloudfront.net
NEXT_PUBLIC_WEBSITE_BUILDER_API_TENANT=root
```

<Callout type="hint">
  All three variables are prefixed with `NEXT_PUBLIC_` because they are used on the client side
  during live editing. The Content SDK also reads them server-side at build/request time.
</Callout>

## Step 4: Start the Dev Server

```bash title="Terminal"
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). At this point you'll see a "Not found!" message — that's expected because there are no pages in your Website Builder yet.

## Step 5: Create Your First Page

1. Open your Webiny Admin app.
2. In the main menu, find **Website Builder** and open it.
3. Click **New Page**.
4. Set the **title** to "Hello World" and the **path** to `/` (the homepage), then click **Create**.

<Image
  src="/images/website-builder/create-page-dialog.png"
  alt="Create a Page dialog with Title set to Hello World and Path set to /"
  caption="Set the title and path, then hit Create"
/>

5. You'll land in the page editor. From the component palette on the left, drag a **Rich Text** component onto the canvas and type "Hello World".

<Image
  src="/images/website-builder/hello-world-typed.png"
  alt="Website Builder editor with Hello World typed into the canvas"
  caption="Hello World typed into the canvas, ready to publish"
/>

6. Click **Publish**.

## Step 6: See It Rendered

Go back to [http://localhost:3000](http://localhost:3000) and refresh. You should now see your "Hello World" text rendered on the page.

<Image
  src="/images/website-builder/hello-world-rendered.png"
  alt="Browser showing the Hello World text rendered from the Website Builder"
  caption="The Hello World page rendered by your Next.js app"
/>

## How the Rendering Works

The starter kit's catch-all route (`src/app/[[...slug]]/page.tsx`) handles all page rendering:

```typescript title="src/app/[[...slug]]/page.tsx (simplified)"
export default async function Page({ params }) {
  const { slug = [] } = await params;

  // Initialize the SDK with credentials from env vars
  initializeContentSdk();

  // Fetch the published page document from Webiny
  const page = await contentSdk.getPage("/" + slug.join("/"));

  // DocumentRenderer maps component names → your React components
  return <DocumentRenderer document={page} />;
}
```

`initializeContentSdk` (in `src/contentSdk/initializeContentSdk.ts`) calls `contentSdk.init()` with your env var credentials and registers the component groups. `DocumentRenderer` takes the page document — a JSON tree of component names and their input values — and renders it by looking up each name in your registered `editorComponents` array.

## Cross-Origin Configuration

Because the editor loads your Next.js app in an iframe, you need to explicitly allow this. The starter kit already handles it — `next.config.ts` reads `NEXT_PUBLIC_WEBSITE_BUILDER_ADMIN_HOST` and adds it to the `Content-Security-Policy` header automatically. `localhost:3001` (the default local Webiny Admin port) is always included.

So if your Admin is on a deployed CloudFront URL, just add it to your `.env`:

```dotenv title=".env"
NEXT_PUBLIC_WEBSITE_BUILDER_ADMIN_HOST=https://your-admin-url.cloudfront.net
```

You can also comma-separate multiple domains if needed:

```dotenv title=".env"
NEXT_PUBLIC_WEBSITE_BUILDER_ADMIN_HOST=https://admin-1.cloudfront.net,https://admin-2.cloudfront.net
```

Without this, the browser will block the iframe and the editor will show a blank canvas.

## Troubleshooting

### Still seeing "Not found!" after publishing

- Make sure you set the page **path** to `/` exactly.
- Check the browser console for API errors — a CORS or auth error means your env vars may be wrong.
- Restart the dev server after editing `.env`.

### Editor shows a blank canvas

- The Admin editor can't load your app in the iframe. Make sure your dev server is running and the CSP header includes the Admin domain.

## Summary

- The Website Builder editor loads your Next.js app in an iframe — components and styles live entirely in your project.
- Cloned the starter kit and configured the three required env vars.
- Created a Hello World page in the Admin editor and saw it rendered in the Next.js app.
- The catch-all route + `initializeContentSdk` + `DocumentRenderer` is how every page gets rendered.

**Next lesson**: Learn how editor components work and how to register your own.

<Quiz
  question="Which environment variable tells the SDK where your Webiny API is hosted?"
  options={[
    "NEXT_PUBLIC_WEBSITE_BUILDER_API_HOST",
    "NEXT_PUBLIC_WEBSITE_BUILDER_API_KEY",
    "NEXT_PUBLIC_WEBSITE_BUILDER_API_TENANT",
    "WEBINY_API_ENDPOINT"
  ]}
  correctAnswer={0}
  explanation="NEXT_PUBLIC_WEBSITE_BUILDER_API_HOST holds the Webiny CloudFront URL (without trailing slash or /graphql path). The API key is a separate variable, and WEBINY_API_ENDPOINT is used by the Headless CMS SDK, not the Website Builder SDK."
  hint="It's the URL you copied from the API Playground."
/>
